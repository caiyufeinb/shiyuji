<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hasee.shiyuji">

    <!-- 允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取GSM（2g）、WCDMA（联通3g）等网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 获取sd卡写的权限，用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许读取手机状态 用于创建BmobInstallation -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".View.MainActivity" />
        <activity android:name=".View.GameActivity" />
        <activity android:name=".View.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.RegisterActivity" />
        <activity android:name=".View.BagActivity" />
        <activity android:name=".View.bag_foodActivity" />
        <activity android:name=".View.questionActivity" />
        <activity android:name=".View.choiseActivity" />
        <activity android:name=".View.ConstitutionActivity" />
        <activity android:name=".View.peaceActivity" />
        <activity android:name=".View.lack_yangActivity" />
        <activity android:name=".View.lack_yinActivity" />
        <activity android:name=".View.phlegm_wetActivity" />
        <activity android:name=".View.wet_hotActivity" />
        <activity android:name=".View.full_of_qiActivity" />
        <activity android:name=".View.lack_of_qiActivity" />
        <activity android:name=".View.bloodActivity" />
        <activity android:name=".View.special_qualityActivity"></activity>
    </application>

</manifest>